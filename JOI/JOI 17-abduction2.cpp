#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int horiz[50001], vert[50001];

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int h, w, q;
    cin >> h >> w >> q;
    for (int i = 1; i <= h; i++) cin >> horiz[i];
    for (int i = 1; i <= w; i++) cin >> vert[i];
    while (q--) {
        int x, y;
        cin >> x >> y;
        
    }
    return 0;
}